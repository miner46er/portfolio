{
  "name": "portfolio",
  "version": "1.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build && next export",
    "start": "next start",
    "lint": "standard",
    "lint:fix": "standard --fix",
    "prepare": "husky install"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "standard --fix"
    ]
  },
  "dependencies": {
    "next": "^12.0.4",
    "sass": "^1.43.5"
  },
  "devDependencies": {
    "husky": "^7.0.4",
    "lint-staged": "^12.1.2",
    "standard": "^16.0.4"
  }
}
